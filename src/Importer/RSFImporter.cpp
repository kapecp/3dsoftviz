#include "Importer/RSFImporter.h"

namespace Importer {

bool RSFImporter::import (
	ImporterContext &context
) {
	throw "not implemented";

	/*
	 * input:
	 * 		stream: context.getStream ()
	 * output:
	 *		graph: context.getGraph ()
	 *		GUI notifications: context.getInfoHandler ()
     */

	return true;
}

} // namespace
